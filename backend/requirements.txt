# ---------------------------------------------------------------------------
# ParcelPilot backend dependencies
# Pinned to versions with prebuilt wheels for Python 3.13 (no compiler needed).
# ---------------------------------------------------------------------------

# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data & validation ---------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36

# --- Postgres driver (production DATABASE_URL; SQLite needs no driver) ---
psycopg[binary]==3.2.3

# --- Agent orchestration -------------------------------------------------
langgraph==0.2.60
langchain-core==0.3.28

# --- HTTP client (OpenRouter / OpenAI-compatible calls) ------------------
httpx==0.28.1

# --- Retrieval -----------------------------------------------------------
numpy==2.2.1
rank-bm25==0.2.2

# --- Data ingestion (seed reads the assessment workbook) -----------------
openpyxl==3.1.5

# --- Utilities -----------------------------------------------------------
tenacity==9.0.0
python-json-logger==3.2.1

# --- Testing -------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
